# Viết lại calibration

> rewrite lại toàn bộ phần calibration, grill me

> rewrite lại toàn bộ phần calibration, grill me

<details><summary>35 previous messages</summary>

> Mình đang ở Plan Mode nên chưa sửa file; trước hết mình sẽ đọc hướng dẫn skill liên quan và soi phần calibration hiện tại để lên kế hoạch rewrite đủ chặt.
>
> <details><summary>Explored 2 files</summary>
>
> - Read `./.agents/skills/using-superpowers/SKILL.md`
> - Read `./.agents/skills/gitnexus-refactoring/SKILL.md`
>
> </details>
>
> Mình sẽ dùng `using-superpowers` và `gitnexus-refactoring`: yêu cầu này là rewrite/refactor một cụm logic, nên cần map phạm vi ảnh hưởng trước khi chốt kế hoạch. Bây giờ mình tìm toàn bộ dấu vết `calibration` và cấu trúc project.
>
> Mình sẽ vừa dùng grep nhanh trong repo vừa hỏi GitNexus để tránh bỏ sót caller/flow động quanh calibration. Mục tiêu là biết phần nào là “calibration” thật sự, không đoán theo tên file.
>
> <details><summary>Explored 1 search, 1 list, ran 2 commands</summary>
>
> - Ran `pwd`
>
> - Ran `git status --short`
>
> <details><summary>Explored 1 search, 1 list</summary>
>
> - Listed files
> - Searched for `calibration|calib|intrinsic|extrinsic|camera|pose|transform|coordinate|scale|axis|world` in `.`
>
> </details>
>
> </details>
>
> Có một điểm cần “grill” ngay: worktree đang rất bẩn và có cả rename/delete liên quan `calibration.ts`/`recipe.ts`; kế hoạch rewrite phải khóa phạm vi để không đè nhầm WIP hiện tại. Mình sẽ thu hẹp search, bỏ file minified, rồi đọc các module calibration/recipe/alignment đang tồn tại.
>
> MCP tool call
>
> gitnexus.list_repos
>
> ```json
> {}
> ```
>
> ```text
> [
>   {
>     "name": "chungnhanso-monorepo",
>     "path": "/home/nqd2/code/chungnhanso-monorepo",
>     "indexedAt": "2026-05-28T04:52:07.821Z",
>     "lastCommit": "3721354181a4f073ab12ba849978a05d86b07871",
>     "stats": {
>       "files": 637,
>       "nodes": 6002,
>       "edges": 10913,
>       "communities": 199,
>       "processes": 184,
>       "embeddings": 0
>     }
>   },
>   {
>     "name": "jv-dine",
>     "path": "/home/nqd2/code/jv-dine",
>     "indexedAt": "2026-05-23T11:47:12.595Z",
>     "lastCommit": "bb7f3b2a7a1e44c82eec7f08e10ab91df92eaa72",
>     "stats": {
>       "files": 181,
>       "nodes": 1141,
>       "edges": 2649,
>       "communities": 50,
>       "processes": 85,
>       "embeddings": 0
>     }
>   },
>   {
>     "name": "z6ds",
>     "path": "/home/nqd2/code/z6ds",
>     "indexedAt": "2026-05-19T09:07:12.828Z",
>     "lastCommit": "4f48ea4638b44a6b894c3e69e6ff9871cf7c3664",
>     "stats": {
>       "files": 419,
>       "nodes": 26477,
>       "edges": 32919,
>       "communities": 981,
>       "processes": 220,
>       "embeddings": 0
>     }
>   },
>   {
>     "name": "codex-app-mint",
>     "path": "/home/nqd2/codex-app-mint",
>     "indexedAt": "2026-05-25T19:37:46.212Z",
>     "lastCommit": "090e0c487334617c382cfb03d7fd0437e5fe88ea",
>     "stats": {
>       "files": 17,
>       "nodes": 154,
>       "edges": 262,
>       "communities": 12,
>       "processes": 11,
>       "embeddings": 0
>     }
>   },
>   {
>     "name": "3DStreetMap",
>     "path": "/home/nqd2/code/3DStreetMap",
>     "indexedAt": "2026-05-28T06:59:20.959Z",
>     "lastCommit": "c3fea8ae3500b188696513d6b2639d63e7982c7e",
>     "stats": {
>       "files": 614,
>       "nodes": 666,
>       "edges": 662,
>       "communities": 0,
>       "processes": 0,
>       "embeddings": 0
>     }
>   },
>   {
>     "name": "2025.2-166155-15",
>     "path": "/home/nqd2/code/ITSSVN/2025.2-166155-15",
>     "indexedAt": "2026-06-01T12:52:59.221Z",
>     "lastCommit": "2167dc36fee53bcaef1063f5f2e7201b4d3820d5",
>     "stats": {
>       "files": 342,
>       "nodes": 1617,
>       "edges": 4348,
>       "communities": 91,
>       "processes": 116,
>       "embeddings": 0
>     }
>   },
>   {
>     "name": "triple-shot-monorepo",
>     "path": "/home/nqd2/code/triple-shot-monorepo",
>     "indexedAt": "2026-06-08T16:47:27.525Z",
>     "lastCommit": "178632213d2bbc74614e2f10d11fe52fb683e267",
>     "stats": {
>       "files": 68,
>       "nodes": 370,
>       "edges": 612,
>       "communities": 21,
>       "processes": 11,
>       "embeddings": 0
>     }
>   }
> ]
>
> ---
> **Next:** READ gitnexus://repo/{name}/context for any repo above to get its overview and check staleness.
> ```
>
> GitNexus không có index cho repo này, nên không thể dùng graph impact trực tiếp; mình sẽ thay bằng `rg` + đọc callers/imports cụ thể. Điểm này cũng phải ghi vào plan: trước rewrite cần tự bảo vệ khỏi đổi nhầm vì không có graph hỗ trợ.
>
> <details><summary>Explored 2 searches, ran 1 command</summary>
>
> - Ran `git diff --name-status -- apps/gui/src crates/core-lib/src tests docs README.md SUMMARY.md DESIGN.md`
>
> <details><summary>Explored 2 searches</summary>
>
> - Searched for `from ['\"].*(calibration|recipe)|import .*calibration|import .*recipe|useCalibration|calibration` in `gui`
> - Searched for `calibration|calib|floor|scale_points|scaleDistance|alignment_recipe|alignmentRecipe|up_axis|upAxis` in `gui`
>
> </details>
>
> </details>
>
> Phạm vi thực tế đang nằm ở 2 lớp: frontend calibration state/geometry (`utils`, `useCalibration`, viewer overlay) và backend alignment bake (`AlignmentRecipe`, `processing/alignment.rs`). Mình đọc thẳng các file đó để xác định rewrite nên dừng ở frontend hay kéo luôn backend contract.
>
> <details><summary>Explored 4 files</summary>
>
> - Read `./apps/gui/src/hooks/useCalibration.ts`
> - Read `./apps/gui/src/utils/calibration.ts`
> - Read `./crates/core-lib/src/processing/alignment.rs`
> - Read `./apps/gui/src/utils/recipe.ts`
>
> </details>
>
> Phần hiện tại có mùi trộn trách nhiệm: math plane/eigen, recipe JSON, localStorage state, picker progression, overlay drawing cùng xoay quanh calibration nhưng nằm rải rác. Mình đọc thêm UI và viewer để xem rewrite nên gom domain module thế nào mà không phá props hiện tại.
>
> <details><summary>Explored 5 files</summary>
>
> - Read `./apps/gui/src/components/ConfigPanel.tsx`
> - Read `./apps/gui/src/utils/calibration.ts`
> - Read `./apps/gui/src/components/Preview.tsx`
> - Read `./apps/gui/src/classes/PlayCanvasViewer.ts`
> - Read `./apps/gui/src/classes/PlayCanvasViewer.ts`
>
> </details>
>
> Tìm thấy một vấn đề lớn hơn “rewrite cho đẹp”: UI đã có mode `plane` và props `planeCoefficients`, nhưng type/hook/recipe hiện tại chưa có contract tương ứng. Nếu rewrite calibration mà không sửa contract này thì TypeScript sẽ tiếp tục vỡ hoặc app serialize sai.
>
> <details><summary>Explored 4 files</summary>
>
> - Read `./apps/gui/src/App.tsx`
> - Read `./apps/gui/src/utils/recipe.test.ts`
> - Read `./apps/gui/src/e2e/gui.spec.ts`
> - Read `./crates/core-lib/src/config.rs`
>
> </details>
>
> Các test đã mô tả rõ intent còn code chưa theo kịp: `recipe.test.ts` đang kỳ vọng `plane` mode, `defaultPlaneCoefficients`, `calibrationPlaneFromCoefficients`, và signature mới của `makeAlignmentRecipe`. Đây là nguồn truth tốt hơn để lập plan.
>
> <details><summary>Explored 3 files, 1 search</summary>
>
> - Read `./Cargo.toml`
> - Read `./apps/gui/package.json`
> - Read `./README.md`
> - Searched for `PlaneCoefficients|defaultPlaneCoefficients|calibrationPlaneFromCoefficients|floorMode|FloorMode` in `gui`
>
> </details>
>
> Mình sẽ chạy unit test GUI để xác nhận lỗi hiện tại, không sửa gì. Kết quả sẽ giúp plan có acceptance criteria thực tế thay vì chỉ dựa trên đọc code.
>
> <details><summary>Ran 1 command</summary>
>
> - Ran `pnpm test`
>
> </details>
>
> Unit test xác nhận lỗi chính: plane mode đang được test/UI kỳ vọng nhưng implementation thiếu hàm normalize plane coefficients. Mình chạy thêm TypeScript build để bắt các mismatch type/props mà unit test runtime chưa bắt hết.
>
> <details><summary>Ran 1 command</summary>
>
> - Ran `pnpm run build`
>
> </details>
>
> Build đang vỡ đúng như nghi ngờ: thiếu export `PlaneCoefficients`, thiếu state/actions trong `useCalibration`, và `makeAlignmentRecipe` signature không còn khớp test. Bây giờ mình sẽ kiểm tra localStorage helper và backend test để chốt cách sửa tối thiểu nhưng sạch.
>
> <details><summary>Explored 1 file, 1 search, ran 1 command</summary>
>
> <details><summary>Explored 1 file, 1 search</summary>
>
> - Read `./apps/gui/src/hooks/useLocalStorage.ts`
> - Searched for `makeAlignmentRecipe\(|calibrationPlaneForMode\(|previewFloorPoints\(|FloorMode|PlaneCoefficients|floorNormalFlipped` in `gui`
>
> </details>
>
> - Ran `cargo test -p augmented-gaussian-core processing::alignment`
>
> </details>
>
> Backend alignment tests đang pass, nên rewrite không nên đụng Rust trừ khi đổi recipe contract. Trọng tâm là hoàn tất frontend calibration domain để UI, unit test, e2e và backend JSON contract cùng nói một ngôn ngữ.
>
> <details><summary>Ran 4 commands</summary>
>
> - Ran `git show HEAD:apps/gui/src/domains/calibration/recipe.ts`
> - Ran `git show HEAD:apps/gui/src/domains/calibration/recipe.test.ts`
> - Ran `git show HEAD:apps/gui/src/domains/calibration/calibration.ts`
> - Ran `git diff --find-renames -- apps/gui/src/utils/calibration.ts apps/gui/src/utils/recipe.ts apps/gui/src/hooks/useCalibration.ts apps/gui/src/components/ConfigPanel.tsx apps/gui/src/App.tsx`
>
> </details>
>
> Một điểm nguy hiểm: các file `apps/gui/src/utils/*` và nhiều component hiện tại đang untracked, nên `git diff` không hiện nội dung thay đổi. Kế hoạch implementation cần coi chúng là WIP của bạn và chỉ chỉnh đúng các file calibration liên quan, không “dọn” status.
>
> <details><summary>Explored 3 lists, ran 1 command</summary>
>
> <details><summary>Explored 3 lists</summary>
>
> - Listed files in `gui`
> - Listed files in `gui`
> - Listed files in `gui`
>
> </details>
>
> - Ran `git status --short --untracked-files=all apps/gui/src | sort`
>
> </details>
>
> HEAD chỉ có calibration/recipe ở `apps/gui/src/*.ts`; WIP đang chuyển sang `apps/gui/src/utils/*.ts` và class/components mới. Mình đọc bản HEAD để biết phần nào là refactor chuyển chỗ, phần nào là tính năng mới còn dang dở.
>
> <details><summary>Ran 4 commands</summary>
>
> - Ran `git show HEAD:apps/gui/src/recipe.test.ts`
> - Ran `git show HEAD:apps/gui/src/recipe.ts`
> - Ran `git show HEAD:apps/gui/src/calibration.ts`
> - Ran `git show HEAD:apps/gui/src/PlayCanvasViewer.ts`
>
> </details>
>
> Trước khi chốt plan, có 2 quyết định ảnh hưởng trực tiếp đến blast radius. Mình có recommendation, nhưng nếu bạn muốn rewrite “toàn bộ” theo nghĩa rộng hơn thì plan sẽ khác hẳn.
>
> User input response
>
> - Calibration rewrite nên chạm phạm vi nào?
>   - End-to-end
> - Muốn đặt calibration code theo cấu trúc nào?
>   - Domain module (Recommended)
>
> Rõ rồi: phạm vi là end-to-end và cấu trúc chính là `domains/calibration`. Với scope này, quyết định lớn nhất là recipe JSON: backend nên hiểu plane equation trực tiếp hay frontend tiếp tục “giả lập” plane bằng 3 điểm.
>
> User input response
>
> - Với plane equation mode, public recipe JSON nên biểu diễn thế nào?
>   - bỏ plane
>
> Plan
>
> # Rewrite Calibration End-To-End
>
> ## Summary
> - Rewrite calibration around a single source of truth: `apps/gui/src/domains/calibration`.
> - Drop `plane` mode completely. Supported floor modes are only `three-point` and `fit`.
> - Keep backend recipe contract focused on `floorPoints`, `floorFitPoints`, `upAxis`, `scalePoints`, `scaleDistanceMeters`, `origin`, and `floorNormalFlipped`.
> - Do not clean unrelated dirty/untracked worktree files.
>
> ## Key Changes
> - Move calibration types, defaults, recipe builders, validation helpers, and plane math into the domain module; update GUI imports from scattered `utils/*` paths.
> - Remove `PlaneCoefficients`, `defaultPlaneCoefficients`, `calibrationPlaneFromCoefficients`, plane editor UI, plane e2e flow, and plane-specific tests.
> - Make `useCalibration` return a coherent object: floor state, scale state, up axis, geometry profile, pick state, active plane, recipe, process config, validity flags, and picker handlers.
> - Keep Rust alignment behavior compatible, but tighten tests/docs around only `floorPoints` and `floorFitPoints`; no native plane-coefficient JSON.
> - Ensure frontend and Rust agree on origin behavior: three-point uses first floor point; fit uses centroid of fit points.
>
> ## Test Plan
> - Run `pnpm test` in `apps/gui`: recipe/calibration tests must pass without plane cases.
> - Run `pnpm run build` in `apps/gui`: TypeScript must compile and Vite build must finish.
> - Run targeted e2e `pnpm run test:e2e -- src/e2e/gui.spec.ts` after removing the plane-mode test.
> - Run `cargo test -p augmented-gaussian-core processing::alignment` and any config/recipe tests touched by the Rust side.
>
> ## Grill / Acceptance Bar
> - Current code is half-migrated: UI expects plane mode, hook/types do not provide it, and tests expect functions that do not exist.
> - The rewrite is not “done” until there is no hidden plane path left in UI options, tests, local state handling, or recipe serialization.
> - Domain module should not become another junk drawer: separate recipe construction, validation, geometry math, and React hook exports clearly.
>
> ## Assumptions
> - “Bỏ plane” means remove plane equation mode rather than hide it temporarily.
> - Fit mode stays fixed at the current four editable fit points for this rewrite.
> - Existing backend JSON names stay camelCase for compatibility.
>
> Plan implementation
>
> Status: completed
> # Rewrite Calibration End-To-End
>
> ## Summary
> - Rewrite calibration around a single source of truth: `apps/gui/src/domains/calibration`.
> - Drop `plane` mode completely. Supported floor modes are only `three-point` and `fit`.
> - Keep backend recipe contract focused on `floorPoints`, `floorFitPoints`, `upAxis`, `scalePoints`, `scaleDistanceMeters`, `origin`, and `floorNormalFlipped`.
> - Do not clean unrelated dirty/untracked worktree files.
>
> ## Key Changes
> - Move calibration types, defaults, recipe builders, validation helpers, and plane math into the domain module; update GUI imports from scattered `utils/*` paths.
> - Remove `PlaneCoefficients`, `defaultPlaneCoefficients`, `calibrationPlaneFromCoefficients`, plane editor UI, plane e2e flow, and plane-specific tests.
> - Make `useCalibration` return a coherent object: floor state, scale state, up axis, geometry profile, pick state, active plane, recipe, process config, validity flags, and picker handlers.
> - Keep Rust alignment behavior compatible, but tighten tests/docs around only `floorPoints` and `floorFitPoints`; no native plane-coefficient JSON.
> - Ensure frontend and Rust agree on origin behavior: three-point uses first floor point; fit uses centroid of fit points.
>
> ## Test Plan
> - Run `pnpm test` in `apps/gui`: recipe/calibration tests must pass without plane cases.
> - Run `pnpm run build` in `apps/gui`: TypeScript must compile and Vite build must finish.
> - Run targeted e2e `pnpm run test:e2e -- src/e2e/gui.spec.ts` after removing the plane-mode test.
> - Run `cargo test -p augmented-gaussian-core processing::alignment` and any config/recipe tests touched by the Rust side.
>
> ## Grill / Acceptance Bar
> - Current code is half-migrated: UI expects plane mode, hook/types do not provide it, and tests expect functions that do not exist.
> - The rewrite is not “done” until there is no hidden plane path left in UI options, tests, local state handling, or recipe serialization.
> - Domain module should not become another junk drawer: separate recipe construction, validation, geometry math, and React hook exports clearly.
>
> ## Assumptions
> - “Bỏ plane” means remove plane equation mode rather than hide it temporarily.
> - Fit mode stays fixed at the current four editable fit points for this rewrite.
> - Existing backend JSON names stay camelCase for compatibility.
</details>

> liệt kê toàn bộ các tính năng hiện tại và clean up những thứ thừa

<details><summary>22 previous messages</summary>

> Mình đang ở Plan Mode nên sẽ không xóa/sửa file ngay. Mình sẽ audit repo trước: liệt kê feature đang có, tìm phần dư/thừa bằng test/build/status, rồi chốt kế hoạch cleanup đủ rõ để thực hiện sau.
>
> <details><summary>Explored 2 files</summary>
>
> - Read `./.agents/skills/ponyail-audit/SKILL.md`
> - Read `./.agents/skills/gitnexus-refactoring/SKILL.md`
>
> </details>
>
> Mình bị gõ sai đường dẫn skill audit, chưa đọc được; sẽ đọc lại đúng file rồi tiếp tục bằng các lệnh chỉ đọc.
>
> <details><summary>Explored 1 file</summary>
>
> - Read `./.agents/skills/ponytail-audit/SKILL.md`
>
> </details>
>
> Mình sẽ lập inventory từ nguồn tin cậy trước: README/SUMMARY, CLI entrypoint, Rust config/pipeline, Tauri commands, React app, tests và danh sách file. Sau đó mới phân loại phần “thừa” thành xóa hẳn, gom module, hoặc giữ vì đang là feature thật.
>
> <details><summary>Explored 4 files</summary>
>
> - Read `./README.md`
> - Read `./SUMMARY.md`
> - Read `./crates/cli/src/main.rs`
> - Read `./crates/core-lib/src/config.rs`
>
> </details>
>
> Từ docs/code đã thấy feature set khá rộng: input readers, alignment/scale calibration, filters, CPU/GPU voxelization, fill/carve, mesh/navmesh, WebAR export, CLI benchmark, Tauri GUI/editor. Mình sẽ đọc tiếp entrypoints Tauri/Rust pipeline và test names để phân biệt feature thật với nhánh dư.
>
> <details><summary>Explored 4 files</summary>
>
> - Read `./apps/gui/src-tauri/src/commands.rs`
> - Read `./crates/core-lib/src/pipeline.rs`
> - Read `./apps/gui/src-tauri/src/commands/source.rs`
> - Read `./apps/gui/src-tauri/src/commands/job.rs`
>
> </details>
>
> Tauri `load_source` hiện chỉ nhận `.ply/.splat`, trong khi core pipeline nhận thêm `.sog/meta.json`; đó là một điểm lệch feature đáng ghi rõ. Mình sẽ rà GUI/editor export và danh sách test để thấy feature nào đang có người dùng/chứng cứ test chống lưng.
>
> <details><summary>Explored 3 files, 1 list</summary>
>
> - Listed files in `editor_export`
> - Read `./apps/gui/src-tauri/src/commands/editor_export/service.rs`
> - Read `./apps/gui/src-tauri/src/commands/editor_export/mod.rs`
> - Read `./apps/gui/src-tauri/src/commands/webar.rs`
>
> </details>
>
> Editor export có hai đường: export server-side từ file gốc và chunked export store. Cần kiểm tra frontend có gọi chunked export không; nếu không, đó là ứng viên cleanup mạnh.
>
> <details><summary>Explored 3 searches, 2 lists, ran 3 commands</summary>
>
> <details><summary>Explored 3 searches, 2 lists</summary>
>
> - Searched for `begin_editor_source_export|append_editor_source_export|finish_editor_source_export|cancel_editor_source_export|export_edited_source|open_webar_viewer|preview_metadata|load_source|process_job|save_bundle|cancel_job` in `gui`
> - Listed files in `docs`
> - Searched for `test\(|it\(|#\[test\]|fn .*test|describe\(` in `crates`
> - Searched for `./target` in `.`
> - Listed files in `examples`
>
> </details>
>
> - Ran `cat .gitignore 2>/dev/null || true`
>
> - Ran `git status --short --untracked-files=all | sed -n '1,220p'`
>
> - Ran `du -sh docs/examples docs/evaluation/results reference 2>/dev/null || true`
>
> </details>
>
> Có dung lượng thừa rõ ràng ở workspace: `docs/examples` 2.1GB và `reference` 820MB đều bị `.gitignore` ignore, còn `docs/evaluation/results` chỉ track manifest/summary nhưng thư mục đang chứa thêm artifact lớn. Đây là cleanup khác loại với cleanup code, nên cần tách trong plan.
>
> <details><summary>Explored 4 files</summary>
>
> - Read `./apps/gui/src/hooks/useSourceLoader.ts`
> - Read `./apps/gui/src/domains/editor/useEditorScene.ts`
> - Read `./crates/core-lib/src/lib.rs`
> - Read `./apps/gui/src/hooks/useBakeJob.ts`
>
> </details>
>
> Một vài cleanup candidate đã có bằng chứng: chunked editor export commands không có caller frontend; `preview_metadata` cũng không có caller frontend; `theme-neutral` có thể là dependency thừa nếu không import. Mình sẽ rà các symbol/dependency kiểu này bằng `rg`.
>
> <details><summary>Explored 4 files, 3 searches, 1 list</summary>
>
> - Searched for `from ['\"]@astryxdesign/theme-neutral|@astryxdesign/theme-neutral|theme-neutral` in `gui`
> - Searched for `theme-neutral|KeyboardFlyInput|E2eSelector|preview_metadata|begin_editor_source_export|append_editor_source_export|finish_editor_source_export|cancel_editor_source_export|defaultConfig|SelectAll|SelectNone|FilterOpacity|FilterBox|FilterSphere|FilterFloatersByVoxelContribution|VoxelBackend::Gpu|compare_cpu_gpu|MeshMode::Faces|FillMode::None` in `.`
> - Searched for `KeyboardFlyInput` in `gui`
> - Listed files in `gui`
> - Read `./crates/core-lib/src/readers/mod.rs`
> - Read `./crates/core-lib/src/output/manifest.rs`
> - Read `./crates/core-lib/src/pipeline.rs`
> - Read `./tests/config/basic.json`
>
> </details>
>
> Feature inventory sơ bộ: core/CLI khá đầy đủ; GUI hiện có Bake + Editor tab + preview; nhưng build đang hỏng vì plane mode dở dang. Cleanup lớn nhất cần quyết định là giữ Editor tab hay quay về GUI bake/calibration đơn giản, và có xóa artifact local nặng không.
>
> User input response
>
> - Cleanup nên giữ phạm vi feature nào làm sản phẩm chính?
>   - Bake GUI + core (Recommended)
> - Có đưa cleanup artifact local nặng vào plan không?
>   - Delete ignored artifacts (Recommended)
>
> Quyết định đã rõ: giữ core/CLI + GUI Bake, giữ Editor vì nó đang là bước transform/export trước bake; cleanup sẽ tập trung vào plane mode dở dang, command/dependency thừa, và artifact ignored nặng. Mình đang gom lại thành plan thực thi cụ thể.
>
> <details><summary>Explored 3 files, ran 1 command</summary>
>
> <details><summary>Explored 3 files</summary>
>
> - Read `./apps/gui/src-tauri/src/commands/editor_export/chunk_store.rs`
> - Read `./apps/gui/src-tauri/src/commands/editor_export/temp.rs`
> - Read `./apps/gui/src-tauri/src/lib.rs`
>
> </details>
>
> - Ran `git ls-files docs/evaluation/results && find docs/evaluation/results -type f | sort | comm -13 <(git ls-files docs/evaluation/results | sort) - | sed -n '1,120p'`
>
> </details>
>
> Plan
>
> # Feature Inventory + Cleanup Plan
>
> ## Tính Năng Hiện Tại
> - Core/CLI: đọc `.ply`, `.splat`, `.sog`/`meta.json`; chạy `process`, `benchmark`, `generate-bench-scenes`.
> - Calibration/alignment: 3 floor points, fitted floor points, up axis, scale endpoints, real-world scale distance, origin, normal flip.
> - Processing: NaN filter, opacity/box/sphere/cluster/floater filters, CPU/GPU voxelization, CPU/GPU parity metrics.
> - Geometry: fill modes `none`, `exterior-fill`, `floor-fill`; capsule carve; mesh `faces` hoặc `smooth`; navmesh bằng `rerecast`.
> - Export: `manifest.json`, `scene.sog`, `collision_mesh.json`, `occlusion.glb`, optional `navmesh.glb/bin`, `index.html`, `webar.zip`.
> - GUI/Tauri: load source, PlayCanvas preview, pick calibration points, Editor transform trước bake, bake progress/cancel, save ZIP, open WebAR preview.
> - Evaluation/tests: Rust unit/e2e, GUI unit/e2e, WebAR smoke, benchmark manifests.
>
> ## Cleanup Chính
> - Xóa hoàn toàn plane equation mode dở dang: bỏ UI option `plane`, `PlaneCoefficients`, plane editor props, plane e2e test, và các test/helper kỳ vọng `calibrationPlaneFromCoefficients`.
> - Chốt module structure: đưa calibration về `apps/gui/src/domains/calibration`; cập nhật imports; xóa bản trùng trong `utils/calibration.ts`, `utils/recipe.ts`, `utils/recipe.test.ts`.
> - Chốt viewer structure theo design: dùng `apps/gui/src/domains/viewer`; không giữ song song `classes/*` và `domains/viewer/*`.
> - Xóa Tauri command thừa không có frontend caller: `preview_metadata` và nhóm chunked editor export `begin/append/finish/cancel_editor_source_export`; giữ `export_edited_source`.
> - Dọn backend recipe dead code: xóa no-op `SelectAll`/`SelectNone`; giữ các filter thật đang documented.
> - Gỡ dependency thừa `@astryxdesign/theme-neutral` khỏi direct deps nếu build vẫn pass.
> - Cập nhật README: danh sách command thực tế, GUI chỉ support `.ply/.splat`, core support thêm `.sog/meta.json`, bỏ mọi nhắc đến plane mode.
>
> ## Cleanup Artifact
> - Xóa ignored local artifacts bằng target rõ ràng, sau dry-run: `docs/examples`, untracked generated files trong `docs/evaluation/results`, và `reference`.
> - Giữ tracked benchmark summaries/manifests trong `docs/evaluation/results`.
> - Không xóa bundled `crates/core-lib/src/output/webar/playcanvas.min.js` vì export pipeline cần nó.
>
> ## Test Plan
> - `cd apps/gui && pnpm test`
> - `cd apps/gui && pnpm run build`
> - `cd apps/gui && pnpm run test:e2e -- src/e2e/gui.spec.ts`
> - `cargo test --workspace`
> - Dry-run artifact cleanup trước khi xóa: `git clean -ndX docs/examples reference docs/evaluation/results`
>
> ## Assumptions
> - Giữ GUI Bake + Editor transform; không cắt Editor tab.
> - “Clean up thừa” bao gồm code thừa và ignored artifact nặng.
> - Không động vào thay đổi unrelated ngoài phạm vi cleanup trên.
>
> Ước lượng ponytail: xóa khoảng vài trăm dòng code/API thừa, một direct dependency, và khoảng 3GB ignored local artifacts.
>
> Plan implementation
>
> Status: completed
> # Feature Inventory + Cleanup Plan
>
> ## Tính Năng Hiện Tại
> - Core/CLI: đọc `.ply`, `.splat`, `.sog`/`meta.json`; chạy `process`, `benchmark`, `generate-bench-scenes`.
> - Calibration/alignment: 3 floor points, fitted floor points, up axis, scale endpoints, real-world scale distance, origin, normal flip.
> - Processing: NaN filter, opacity/box/sphere/cluster/floater filters, CPU/GPU voxelization, CPU/GPU parity metrics.
> - Geometry: fill modes `none`, `exterior-fill`, `floor-fill`; capsule carve; mesh `faces` hoặc `smooth`; navmesh bằng `rerecast`.
> - Export: `manifest.json`, `scene.sog`, `collision_mesh.json`, `occlusion.glb`, optional `navmesh.glb/bin`, `index.html`, `webar.zip`.
> - GUI/Tauri: load source, PlayCanvas preview, pick calibration points, Editor transform trước bake, bake progress/cancel, save ZIP, open WebAR preview.
> - Evaluation/tests: Rust unit/e2e, GUI unit/e2e, WebAR smoke, benchmark manifests.
>
> ## Cleanup Chính
> - Xóa hoàn toàn plane equation mode dở dang: bỏ UI option `plane`, `PlaneCoefficients`, plane editor props, plane e2e test, và các test/helper kỳ vọng `calibrationPlaneFromCoefficients`.
> - Chốt module structure: đưa calibration về `apps/gui/src/domains/calibration`; cập nhật imports; xóa bản trùng trong `utils/calibration.ts`, `utils/recipe.ts`, `utils/recipe.test.ts`.
> - Chốt viewer structure theo design: dùng `apps/gui/src/domains/viewer`; không giữ song song `classes/*` và `domains/viewer/*`.
> - Xóa Tauri command thừa không có frontend caller: `preview_metadata` và nhóm chunked editor export `begin/append/finish/cancel_editor_source_export`; giữ `export_edited_source`.
> - Dọn backend recipe dead code: xóa no-op `SelectAll`/`SelectNone`; giữ các filter thật đang documented.
> - Gỡ dependency thừa `@astryxdesign/theme-neutral` khỏi direct deps nếu build vẫn pass.
> - Cập nhật README: danh sách command thực tế, GUI chỉ support `.ply/.splat`, core support thêm `.sog/meta.json`, bỏ mọi nhắc đến plane mode.
>
> ## Cleanup Artifact
> - Xóa ignored local artifacts bằng target rõ ràng, sau dry-run: `docs/examples`, untracked generated files trong `docs/evaluation/results`, và `reference`.
> - Giữ tracked benchmark summaries/manifests trong `docs/evaluation/results`.
> - Không xóa bundled `crates/core-lib/src/output/webar/playcanvas.min.js` vì export pipeline cần nó.
>
> ## Test Plan
> - `cd apps/gui && pnpm test`
> - `cd apps/gui && pnpm run build`
> - `cd apps/gui && pnpm run test:e2e -- src/e2e/gui.spec.ts`
> - `cargo test --workspace`
> - Dry-run artifact cleanup trước khi xóa: `git clean -ndX docs/examples reference docs/evaluation/results`
>
> ## Assumptions
> - Giữ GUI Bake + Editor transform; không cắt Editor tab.
> - “Clean up thừa” bao gồm code thừa và ignored artifact nặng.
> - Không động vào thay đổi unrelated ngoài phạm vi cleanup trên.
>
> Ước lượng ponytail: xóa khoảng vài trăm dòng code/API thừa, một direct dependency, và khoảng 3GB ignored local artifacts.
</details>

> Đề tài: Nghiên cứu, phát triển công cụ xử lý dữ liệu 3D Gaussian Splatting phục vụ hệ thống Thực tế tăng cường (AR)
> Mô tả: Là một công cụ tự động xử lý và chuyển đổi dữ liệu 3D Gaussian Splatting (3DGS) thành các mô hình 3D có cấu trúc hình học. Mục tiêu là giúp các vật thể ảo trong môi trường AR có thể nhận biết được vật cản và nhận diện được địa hình trong thực tế.
> Yêu cầu chức năng:
>  - Tiền xử lý dữ liệu: Đọc file dữ liệu 3DGS đầu vào (định dạng .ply hoặc .splat). Nhận diện và lọc bỏ các điểm ảnh bị nhiễu, mờ hoặc thừa thãi trong không gian quét.
>  - Tái tạo và tối ưu hóa hình học: Chuyển đổi đám mây điểm 3DGS thành bề mặt lưới 3D (Mesh). Tối ưu hóa và làm nhẹ lưới 3D này để giảm thiểu dung lượng tải.
>  - Trích xuất dữ liệu phục vụ AR: Tạo lưới che khuất (Occlusion Mesh) làm vật cản và lưới điều hướng (Navigation Mesh) làm bản đồ tìm đường.
>  - Xuất dữ liệu: Xuất file mô hình kết quả cuối cùng dưới định dạng 3D chuẩn phổ biến, tối ưu cho môi trường web/AR (ví dụ: .glb).
> Yêu cầu phi chức năng:
>  - Triển khai: Đóng gói thành công cụ chạy tự động qua dòng lệnh (CLI) hoặc có giao diện người dùng đơn giản, dễ thao tác.
>  - Chất lượng đầu ra: Đảm bảo mô hình xuất ra khớp chính xác về tỷ lệ và tọa độ so với thế giới thực, đồng thời đủ nhẹ để tải mượt mà trên ứng dụng.
>
> hiện tại project đã hoàn thành được phần nào rồi?

<details><summary>3 previous messages</summary>

> Mình sẽ trả lời theo trạng thái code hiện tại đã audit: so với đề tài, phần core xử lý đã đi khá xa, còn GUI/calibration đang có WIP chưa sạch và build frontend đang hỏng ở plane mode.
>
> <details><summary>Explored 1 file</summary>
>
> - Read `./.agents/skills/gitnexus-exploring/SKILL.md`
>
> </details>
>
> Tóm tắt thẳng: **core xử lý đã làm được phần lớn đề tài**, nhưng **GUI/calibration đang WIP và chưa ship-ready**.
>
> **Đã hoàn thành khá rõ**
> - Đọc dữ liệu 3DGS: core đọc `.ply`, `.splat`, thêm cả `.sog/meta.json`; GUI hiện load `.ply/.splat`.
> - Tiền xử lý/lọc: có filter NaN, opacity, box/sphere, cluster, floater-by-voxel-contribution.
> - Calibration: có floor points, fitted floor points, up axis, scale endpoints, origin, scale theo mét.
> - Tái tạo hình học: pipeline voxel hóa CPU/GPU, fill/carve, xuất mesh dạng `faces` hoặc `smooth`.
> - AR output: có `occlusion.glb`, `navmesh.glb`, `navmesh.bin`, `collision_mesh.json`.
> - Export WebAR: có `scene.sog`, `index.html`, PlayCanvas runtime, `webar.zip`, `manifest.json`.
> - CLI: có `process`, `benchmark`, `generate-bench-scenes`.
> - Benchmark/evaluation: có CPU/GPU parity metrics, geometric error metrics, benchmark output manifests.
>
> **Hoàn thành một phần**
> - “Nhận diện điểm nhiễu/mờ/thừa”: hiện là heuristic/filter hình học và voxel contribution, chưa phải thuật toán nhận diện mờ/nhiễu nâng cao.
> - “Tối ưu/làm nhẹ mesh”: có smooth/Marching Cubes và merge mặt phẳng, nhưng chưa có decimation/LOD/mesh simplification chuyên dụng.
> - “Khớp tỷ lệ/toạ độ thế giới thực”: có calibration scale/origin/up-axis, nhưng chất lượng phụ thuộc người pick điểm và GUI calibration hiện chưa sạch.
> - GUI: đã có React/Tauri app, preview PlayCanvas, editor transform, bake/cancel/save/preview, nhưng code đang nửa refactor.
>
> **Chưa ổn / cần cleanup trước khi gọi là hoàn thành**
> - Frontend build hiện đang hỏng vì `plane` calibration mode dở dang: UI/test expect `PlaneCoefficients` và `calibrationPlaneFromCoefficients`, nhưng implementation chưa có.
> - Có code thừa không có caller: `preview_metadata`, chunked editor export commands.
> - Có module trùng/đang migrate: `utils/calibration.ts`, `utils/recipe.ts` vs hướng `domains/calibration`.
> - Worktree đang rất bẩn với nhiều rename/untracked file, nên chưa thể coi là trạng thái release.
>
> Đánh giá thực tế: **backend/core khoảng 75-85% yêu cầu đề tài**, **GUI khoảng 50-60%**, **toàn project khoảng 70% nhưng chưa đủ sạch để demo nghiêm túc nếu chưa cleanup calibration/build**.
</details>

Tóm tắt thẳng: **core xử lý đã làm được phần lớn đề tài**, nhưng **GUI/calibration đang WIP và chưa ship-ready**.

**Đã hoàn thành khá rõ**
- Đọc dữ liệu 3DGS: core đọc `.ply`, `.splat`, thêm cả `.sog/meta.json`; GUI hiện load `.ply/.splat`.
- Tiền xử lý/lọc: có filter NaN, opacity, box/sphere, cluster, floater-by-voxel-contribution.
- Calibration: có floor points, fitted floor points, up axis, scale endpoints, origin, scale theo mét.
- Tái tạo hình học: pipeline voxel hóa CPU/GPU, fill/carve, xuất mesh dạng `faces` hoặc `smooth`.
- AR output: có `occlusion.glb`, `navmesh.glb`, `navmesh.bin`, `collision_mesh.json`.
- Export WebAR: có `scene.sog`, `index.html`, PlayCanvas runtime, `webar.zip`, `manifest.json`.
- CLI: có `process`, `benchmark`, `generate-bench-scenes`.
- Benchmark/evaluation: có CPU/GPU parity metrics, geometric error metrics, benchmark output manifests.

**Hoàn thành một phần**
- “Nhận diện điểm nhiễu/mờ/thừa”: hiện là heuristic/filter hình học và voxel contribution, chưa phải thuật toán nhận diện mờ/nhiễu nâng cao.
- “Tối ưu/làm nhẹ mesh”: có smooth/Marching Cubes và merge mặt phẳng, nhưng chưa có decimation/LOD/mesh simplification chuyên dụng.
- “Khớp tỷ lệ/toạ độ thế giới thực”: có calibration scale/origin/up-axis, nhưng chất lượng phụ thuộc người pick điểm và GUI calibration hiện chưa sạch.
- GUI: đã có React/Tauri app, preview PlayCanvas, editor transform, bake/cancel/save/preview, nhưng code đang nửa refactor.

**Chưa ổn / cần cleanup trước khi gọi là hoàn thành**
- Frontend build hiện đang hỏng vì `plane` calibration mode dở dang: UI/test expect `PlaneCoefficients` và `calibrationPlaneFromCoefficients`, nhưng implementation chưa có.
- Có code thừa không có caller: `preview_metadata`, chunked editor export commands.
- Có module trùng/đang migrate: `utils/calibration.ts`, `utils/recipe.ts` vs hướng `domains/calibration`.
- Worktree đang rất bẩn với nhiều rename/untracked file, nên chưa thể coi là trạng thái release.

Đánh giá thực tế: **backend/core khoảng 75-85% yêu cầu đề tài**, **GUI khoảng 50-60%**, **toàn project khoảng 70% nhưng chưa đủ sạch để demo nghiêm túc nếu chưa cleanup calibration/build**.

> soạn plan finish project

<details><summary>4 previous messages</summary>

> Mình sẽ dùng `writing-plans` vì bạn đang yêu cầu plan hoàn thiện project. Mình đã có audit feature/cleanup từ các lượt trước, chỉ đọc thêm skill rồi chốt thành plan thực thi.
>
> <details><summary>Explored 1 file</summary>
>
> - Read `./.agents/skills/writing-plans/SKILL.md`
>
> </details>
>
> Plan
>
> # Finish Augmented Gaussian Project
>
> ## Summary
> - Mục tiêu hoàn thiện: demo được từ `.ply/.splat` -> calibration -> filtering -> mesh/navmesh -> `.glb`/WebAR bundle qua CLI và GUI.
> - Ưu tiên: làm project sạch, build/test pass, bỏ feature dở dang, giữ scope đúng đề tài.
> - Không mở rộng sang nghiên cứu mới như ML denoise hay semantic AR; chỉ hoàn thiện pipeline hiện có thành bản demo tốt.
>
> ## Phase 1: Clean Up Project
> - Xóa hoàn toàn `plane` calibration mode đang dở dang.
> - Gom calibration về một domain module duy nhất: `apps/gui/src/domains/calibration`.
> - Không giữ song song `utils/calibration.ts` và `domains/calibration`.
> - Xóa Tauri commands không dùng: `preview_metadata`, chunked editor export commands.
> - Giữ `export_edited_source` vì Editor transform đang phục vụ bake.
> - Xóa dependency không dùng `@astryxdesign/theme-neutral`.
> - Dọn ignored artifacts nặng: `docs/examples`, generated files trong `docs/evaluation/results`, `reference`.
>
> ## Phase 2: Finish Calibration
> - Giữ 2 floor modes:
>   - `three-point`: chọn 3 điểm sàn.
>   - `fit`: chọn nhiều điểm để fit mặt phẳng sàn.
> - Giữ scale calibration bằng 2 điểm + khoảng cách mét.
> - Recipe gửi backend phải ổn định:
>   - `floorPoints` hoặc `floorFitPoints`
>   - `upAxis`
>   - `scalePoints`
>   - `scaleDistanceMeters`
>   - `origin`
>   - `floorNormalFlipped`
> - GUI chỉ cho Bake khi source loaded, floor valid, scale valid, scene visible.
>
> ## Phase 3: Finish Processing Quality
> - Giữ input chính theo đề tài: `.ply`, `.splat`.
> - Core có thể tiếp tục support `.sog/meta.json`, nhưng GUI/docs không quảng bá nếu chưa cần.
> - Đảm bảo filter pipeline hoạt động:
>   - NaN filter
>   - opacity filter
>   - cluster filter
>   - floater contribution filter
> - Mesh optimization đủ cho demo:
>   - voxel size configurable
>   - `faces` và `smooth`
>   - coplanar merge
>   - metrics triangle count + file size ratio trong manifest
> - Không thêm decimation phức tạp trừ khi smooth mesh vẫn quá nặng trong demo.
>
> ## Phase 4: Finish AR Outputs
> - Bắt buộc output:
>   - `manifest.json`
>   - `scene.sog`
>   - `collision_mesh.json`
>   - `occlusion.glb`
>   - `index.html`
>   - `webar.zip`
> - Optional output:
>   - `navmesh.glb`
>   - `navmesh.bin`
> - Nếu không có navmesh hợp lệ, manifest để `null` và xóa stale navmesh files.
> - WebAR viewer phải load được toàn bộ assets declared trong manifest.
>
> ## Phase 5: Finish GUI Demo
> - GUI workflow cuối cùng:
>   1. Load `.ply/.splat`
>   2. Editor transform nếu cần
>   3. Chọn floor mode
>   4. Pick floor points
>   5. Pick scale endpoints
>   6. Nhập distance meters
>   7. Chọn profile: interior/outdoor
>   8. Bake
>   9. Preview WebAR hoặc Save ZIP
> - UI không còn option chết, không còn control không serialize được.
> - Status panel hiển thị source, progress, output manifest, warnings.
>
> ## Phase 6: Docs + Evaluation
> - README cập nhật đúng trạng thái thật.
> - Thêm section “Completed requirements” mapping với đề tài.
> - Giữ benchmark summary/manifests nhỏ.
> - Viết demo script:
>   - CLI command
>   - GUI workflow
>   - expected output files
>   - troubleshooting nếu navmesh rỗng.
> - Chuẩn bị ảnh/video demo nếu cần bảo vệ đề tài.
>
> ## Test Plan
> - Rust:
>   - `cargo test --workspace`
>   - CLI e2e với `tests/fixtures/minimal.splat`
> - GUI:
>   - `cd apps/gui && pnpm test`
>   - `cd apps/gui && pnpm run build`
>   - `cd apps/gui && pnpm run test:e2e`
> - Manual demo:
>   - Load `.splat`
>   - Pick calibration
>   - Bake
>   - Confirm `occlusion.glb`, optional `navmesh.glb`, `webar.zip`
>   - Open WebAR preview.
>
> ## Definition Of Done
> - Clean `cargo test --workspace`.
> - Clean GUI test/build/e2e.
> - No dead `plane` calibration references.
> - No unused Tauri commands exposed.
> - README matches actual features.
> - One sample input can produce final AR bundle end-to-end.
> - Project explanation clearly maps to every functional/non-functional requirement.
>
> ## Assumptions
> - Final product giữ cả CLI và GUI.
> - Editor transform được giữ vì hữu ích cho calibration/bake.
> - Scope demo tập trung `.ply/.splat`, occlusion mesh, navmesh, `.glb`, WebAR bundle.
>
> Plan implementation
>
> Status: completed
> # Finish Augmented Gaussian Project
>
> ## Summary
> - Mục tiêu hoàn thiện: demo được từ `.ply/.splat` -> calibration -> filtering -> mesh/navmesh -> `.glb`/WebAR bundle qua CLI và GUI.
> - Ưu tiên: làm project sạch, build/test pass, bỏ feature dở dang, giữ scope đúng đề tài.
> - Không mở rộng sang nghiên cứu mới như ML denoise hay semantic AR; chỉ hoàn thiện pipeline hiện có thành bản demo tốt.
>
> ## Phase 1: Clean Up Project
> - Xóa hoàn toàn `plane` calibration mode đang dở dang.
> - Gom calibration về một domain module duy nhất: `apps/gui/src/domains/calibration`.
> - Không giữ song song `utils/calibration.ts` và `domains/calibration`.
> - Xóa Tauri commands không dùng: `preview_metadata`, chunked editor export commands.
> - Giữ `export_edited_source` vì Editor transform đang phục vụ bake.
> - Xóa dependency không dùng `@astryxdesign/theme-neutral`.
> - Dọn ignored artifacts nặng: `docs/examples`, generated files trong `docs/evaluation/results`, `reference`.
>
> ## Phase 2: Finish Calibration
> - Giữ 2 floor modes:
>   - `three-point`: chọn 3 điểm sàn.
>   - `fit`: chọn nhiều điểm để fit mặt phẳng sàn.
> - Giữ scale calibration bằng 2 điểm + khoảng cách mét.
> - Recipe gửi backend phải ổn định:
>   - `floorPoints` hoặc `floorFitPoints`
>   - `upAxis`
>   - `scalePoints`
>   - `scaleDistanceMeters`
>   - `origin`
>   - `floorNormalFlipped`
> - GUI chỉ cho Bake khi source loaded, floor valid, scale valid, scene visible.
>
> ## Phase 3: Finish Processing Quality
> - Giữ input chính theo đề tài: `.ply`, `.splat`.
> - Core có thể tiếp tục support `.sog/meta.json`, nhưng GUI/docs không quảng bá nếu chưa cần.
> - Đảm bảo filter pipeline hoạt động:
>   - NaN filter
>   - opacity filter
>   - cluster filter
>   - floater contribution filter
> - Mesh optimization đủ cho demo:
>   - voxel size configurable
>   - `faces` và `smooth`
>   - coplanar merge
>   - metrics triangle count + file size ratio trong manifest
> - Không thêm decimation phức tạp trừ khi smooth mesh vẫn quá nặng trong demo.
>
> ## Phase 4: Finish AR Outputs
> - Bắt buộc output:
>   - `manifest.json`
>   - `scene.sog`
>   - `collision_mesh.json`
>   - `occlusion.glb`
>   - `index.html`
>   - `webar.zip`
> - Optional output:
>   - `navmesh.glb`
>   - `navmesh.bin`
> - Nếu không có navmesh hợp lệ, manifest để `null` và xóa stale navmesh files.
> - WebAR viewer phải load được toàn bộ assets declared trong manifest.
>
> ## Phase 5: Finish GUI Demo
> - GUI workflow cuối cùng:
>   1. Load `.ply/.splat`
>   2. Editor transform nếu cần
>   3. Chọn floor mode
>   4. Pick floor points
>   5. Pick scale endpoints
>   6. Nhập distance meters
>   7. Chọn profile: interior/outdoor
>   8. Bake
>   9. Preview WebAR hoặc Save ZIP
> - UI không còn option chết, không còn control không serialize được.
> - Status panel hiển thị source, progress, output manifest, warnings.
>
> ## Phase 6: Docs + Evaluation
> - README cập nhật đúng trạng thái thật.
> - Thêm section “Completed requirements” mapping với đề tài.
> - Giữ benchmark summary/manifests nhỏ.
> - Viết demo script:
>   - CLI command
>   - GUI workflow
>   - expected output files
>   - troubleshooting nếu navmesh rỗng.
> - Chuẩn bị ảnh/video demo nếu cần bảo vệ đề tài.
>
> ## Test Plan
> - Rust:
>   - `cargo test --workspace`
>   - CLI e2e với `tests/fixtures/minimal.splat`
> - GUI:
>   - `cd apps/gui && pnpm test`
>   - `cd apps/gui && pnpm run build`
>   - `cd apps/gui && pnpm run test:e2e`
> - Manual demo:
>   - Load `.splat`
>   - Pick calibration
>   - Bake
>   - Confirm `occlusion.glb`, optional `navmesh.glb`, `webar.zip`
>   - Open WebAR preview.
>
> ## Definition Of Done
> - Clean `cargo test --workspace`.
> - Clean GUI test/build/e2e.
> - No dead `plane` calibration references.
> - No unused Tauri commands exposed.
> - README matches actual features.
> - One sample input can produce final AR bundle end-to-end.
> - Project explanation clearly maps to every functional/non-functional requirement.
>
> ## Assumptions
> - Final product giữ cả CLI và GUI.
> - Editor transform được giữ vì hữu ích cho calibration/bake.
> - Scope demo tập trung `.ply/.splat`, occlusion mesh, navmesh, `.glb`, WebAR bundle.
</details>

> Finish Augmented Gaussian Project
> This plan addresses all remaining requirements to complete the project for the demo, ensuring it cleanly processes .ply/.splat files to WebAR outputs with proper calibration, occlusion, and navigation meshes.
>
> User Review Required
> Please review the proposed backend alignment changes. Since floorPoints and floorFitPoints are being removed from the recipe, the GUI will compute the exact floor plane normal and send it to the backend as floorNormal alongside the origin. The backend will then trust this normal for alignment.
>
> Open Questions
> [!IMPORTANT] GUI Workflow for Floor Plane Calibration You mentioned keeping the fit mode for the floor, but in our chat, you also said: "No, the floor plane is calculated automatically without picking." Does this mean we should completely remove the UI for picking floor points, and the application will just automatically calculate the floor normal from the entire point cloud (e.g. via PCA)? Or did you mean that the user can still pick points, but the plane is fit automatically from those picked points?
>
> Proposed Changes
> Phase 1: Clean Up Project
> [DELETE] apps/gui/src/utils/calibration.ts
> [DELETE] apps/gui/src/utils/recipe.ts
> [NEW] apps/gui/src/domains/calibration/index.ts
> Move all calibration logic into the domain module, removing the unused plane and three-point modes entirely.
>
> [MODIFY] apps/gui/src-tauri/src/lib.rs and commands.rs
> Remove unused Tauri commands: preview_metadata, begin_editor_source_export, append_editor_source_export, finish_editor_source_export, and cancel_editor_source_export.
>
> [MODIFY] apps/gui/package.json
> Remove the unused @astryxdesign/theme-neutral dependency.
>
> [DELETE] Large/ignored artifacts
> Delete docs/examples, generated files in docs/evaluation/results, and reference.
>
> Phase 2 & 3: Finish Calibration & Processing Quality
> [MODIFY] crates/core-lib/src/config.rs
> Update AlignmentRecipe to remove floor_points, floor_fit_points, and floor_normal_flipped. Add floor_normal.
>
> pub struct AlignmentRecipe {
>     pub up_axis: Option<UpAxis>,
>     pub floor_normal: Option<[f32; 3]>,
>     pub scale_points: Option<[[f32; 3]; 2]>,
>     pub scale_distance_meters: Option<f32>,
>     pub origin: Option<[f32; 3]>,
> }
> [MODIFY] crates/core-lib/src/processing/alignment.rs
> Update the alignment calculation to directly use the provided floor_normal and origin from the recipe, rather than fitting a plane from points.
>
> [MODIFY] apps/gui/src/components/ConfigPanel.tsx
> Update the GUI recipe generation to compute floorNormal and pass it to the backend. Enforce validation so that "Bake" is only enabled when the source is loaded, floor is valid, scale is valid, and the scene is visible.
>
> Phase 4: Finish AR Outputs
> [MODIFY] crates/core-lib/src/pipeline.rs
> Ensure that if a navmesh generates 0 triangles, it is treated as invalid. It will not be exported, any stale navmesh.glb/navmesh.bin files will be deleted, and the manifest will contain null for the navmesh fields.
>
> Phase 5 & 6: GUI Workflow & Docs
> [MODIFY] apps/gui/src/components/ConfigPanel.tsx
> Simplify the GUI workflow. Remove the "Floor Mode" dropdown (as fit will be the only mode) and clean up unserializable UI controls. Add progress, warnings, and output manifest details to StatusPanel.tsx.
>
> [MODIFY] README.md
> Update to reflect the final state of the project. Add a "Completed requirements" section mapping to the original goals. Add a demo script detailing the CLI command, GUI workflow, expected outputs, and troubleshooting.
>
> Verification Plan
> Automated Tests
> Run cargo test --workspace to ensure all backend and CLI tests pass, specifically testing the minimal.splat fixture.
> Run cd apps/gui && pnpm test and pnpm run build to verify the frontend builds cleanly without type errors.
> Manual Verification
> Open the GUI, load a .splat file, perform editor transformations, configure scale endpoints, select a profile, and hit Bake.
> Verify that manifest.json, scene.sog, collision_mesh.json, occlusion.glb, index.html, and webar.zip are correctly generated.
> Verify that if the navmesh is invalid, it correctly outputs null in the manifest and no stale files remain.
>
> ?

<details><summary>5 previous messages</summary>

> Mình sẽ review plan này như một spec trước khi chốt lại: trọng tâm là recipe contract `floorNormal` vs point-based calibration, vì thay contract backend sẽ ảnh hưởng CLI/docs/tests/manifest và không chỉ GUI.
>
> <details><summary>Explored 2 files</summary>
>
> - Read `./.agents/skills/writing-plans/SKILL.md`
> - Read `./.agents/skills/gitnexus-impact-analysis/SKILL.md`
>
> </details>
>
> Mình sẽ sửa hướng plan: **không remove `floorPoints` / `floorFitPoints` khỏi recipe backend**.
>
> Lý do: nếu backend chỉ “trust `floorNormal` từ GUI”, CLI yếu đi, backend mất khả năng tự validate collinear/coincident points, và logic calibration bị dồn sang frontend. Với đề tài/demo, cách tốt nhất là: **user pick điểm, hệ thống tự fit plane từ các điểm đó**. Không nên auto PCA toàn bộ point cloud trong bản finish này, vì dễ bắt nhầm tường/trần/object clutter thành “floor”.
>
> Plan
>
> # Finish Augmented Gaussian Project
>
> ## Summary
> - Giữ pipeline chính: `.ply/.splat` -> calibration -> filter -> mesh/navmesh -> `.glb`/WebAR.
> - Calibration final: không có `plane equation` mode; chỉ có `three-point` và `fit picked points`.
> - Backend vẫn là source of truth cho alignment math từ `floorPoints`/`floorFitPoints`.
> - Không auto detect floor từ toàn bộ point cloud trong bản demo.
>
> ## Key Corrections
> - Không đổi `AlignmentRecipe` sang chỉ có `floorNormal`.
> - Giữ contract backend:
>   - `floorPoints?: [[f32; 3]; 3]`
>   - `floorFitPoints?: Vec<[f32; 3]>`
>   - `upAxis?: UpAxis`
>   - `scalePoints?: [[f32; 3]; 2]`
>   - `scaleDistanceMeters?: f32`
>   - `origin?: [f32; 3]`
>   - `floorNormalFlipped?: bool`
> - GUI có thể tính preview normal để vẽ overlay, nhưng backend tự fit/recompute khi bake.
>
> ## Calibration Workflow
> - Remove `plane equation` UI hoàn toàn.
> - Keep `three-point`: pick 3 floor points, backend computes normal.
> - Keep `fit`: pick 4 floor points hiện tại, backend fits best plane.
> - “Floor plane calculated automatically” nghĩa là auto-fit từ picked points, không phải auto detect toàn bộ cloud.
> - Bake chỉ enabled khi:
>   - source loaded
>   - floor points valid
>   - scale endpoints valid
>   - distance > 0
>   - scene visible
>
> ## Cleanup
> - Move calibration frontend code into `apps/gui/src/domains/calibration`.
> - Remove duplicate/stale `utils/calibration.ts`, `utils/recipe.ts` after imports are updated.
> - Remove unused Tauri commands:
>   - `preview_metadata`
>   - `begin_editor_source_export`
>   - `append_editor_source_export`
>   - `finish_editor_source_export`
>   - `cancel_editor_source_export`
> - Keep `export_edited_source`.
> - Remove unused `@astryxdesign/theme-neutral`.
> - Delete ignored large artifacts: `docs/examples`, untracked generated outputs under `docs/evaluation/results`, `reference`.
>
> ## Backend Finish
> - Keep `processing/alignment.rs` point-based alignment.
> - Add/keep tests for:
>   - scale calibration
>   - three-point floor alignment
>   - fitted floor alignment
>   - collinear fit rejection
>   - normal flip
>   - origin maps floor to zero plane
> - In `pipeline.rs`, if navmesh has 0 triangles:
>   - manifest navmesh fields become `null`
>   - stale `navmesh.glb` / `navmesh.bin` are deleted
>   - warning is recorded
>
> ## GUI Finish
> - Remove Floor Mode option `plane`.
> - Keep Floor Mode dropdown only if both `three-point` and `fit` remain visible.
> - Status panel should show:
>   - source loaded
>   - bake stage
>   - warnings
>   - manifest output
>   - artifact actions: Preview WebAR, Save ZIP
> - Keep Editor transform tab because it supports final bake workflow.
>
> ## Docs
> - README must match final state:
>   - GUI supports `.ply/.splat`
>   - CLI/core may support `.sog/meta.json`
>   - recipe examples use `floorPoints` and `floorFitPoints`
>   - no `floorNormal` public recipe
>   - no plane equation mode
> - Add “Completed requirements” mapping:
>   - preprocessing/filtering
>   - mesh reconstruction
>   - occlusion mesh
>   - navmesh
>   - GLB/WebAR export
>   - CLI/GUI packaging
>   - calibration for real-world scale/coordinates
>
> ## Verification
> - `cargo test --workspace`
> - `cd apps/gui && pnpm test`
> - `cd apps/gui && pnpm run build`
> - `cd apps/gui && pnpm run test:e2e`
> - Manual demo:
>   - load `.splat`
>   - pick floor points
>   - pick scale endpoints
>   - bake
>   - verify `manifest.json`, `scene.sog`, `collision_mesh.json`, `occlusion.glb`, `index.html`, `webar.zip`
>   - verify navmesh null behavior when invalid
>
> ## Assumptions
> - Auto floor detection from entire cloud is out of scope for final demo.
> - Fit mode means “fit automatically from user-picked points”.
> - Backend remains responsible for final calibration math and validation.
>
> Plan implementation
>
> Status: completed
> # Finish Augmented Gaussian Project
>
> ## Summary
> - Giữ pipeline chính: `.ply/.splat` -> calibration -> filter -> mesh/navmesh -> `.glb`/WebAR.
> - Calibration final: không có `plane equation` mode; chỉ có `three-point` và `fit picked points`.
> - Backend vẫn là source of truth cho alignment math từ `floorPoints`/`floorFitPoints`.
> - Không auto detect floor từ toàn bộ point cloud trong bản demo.
>
> ## Key Corrections
> - Không đổi `AlignmentRecipe` sang chỉ có `floorNormal`.
> - Giữ contract backend:
>   - `floorPoints?: [[f32; 3]; 3]`
>   - `floorFitPoints?: Vec<[f32; 3]>`
>   - `upAxis?: UpAxis`
>   - `scalePoints?: [[f32; 3]; 2]`
>   - `scaleDistanceMeters?: f32`
>   - `origin?: [f32; 3]`
>   - `floorNormalFlipped?: bool`
> - GUI có thể tính preview normal để vẽ overlay, nhưng backend tự fit/recompute khi bake.
>
> ## Calibration Workflow
> - Remove `plane equation` UI hoàn toàn.
> - Keep `three-point`: pick 3 floor points, backend computes normal.
> - Keep `fit`: pick 4 floor points hiện tại, backend fits best plane.
> - “Floor plane calculated automatically” nghĩa là auto-fit từ picked points, không phải auto detect toàn bộ cloud.
> - Bake chỉ enabled khi:
>   - source loaded
>   - floor points valid
>   - scale endpoints valid
>   - distance > 0
>   - scene visible
>
> ## Cleanup
> - Move calibration frontend code into `apps/gui/src/domains/calibration`.
> - Remove duplicate/stale `utils/calibration.ts`, `utils/recipe.ts` after imports are updated.
> - Remove unused Tauri commands:
>   - `preview_metadata`
>   - `begin_editor_source_export`
>   - `append_editor_source_export`
>   - `finish_editor_source_export`
>   - `cancel_editor_source_export`
> - Keep `export_edited_source`.
> - Remove unused `@astryxdesign/theme-neutral`.
> - Delete ignored large artifacts: `docs/examples`, untracked generated outputs under `docs/evaluation/results`, `reference`.
>
> ## Backend Finish
> - Keep `processing/alignment.rs` point-based alignment.
> - Add/keep tests for:
>   - scale calibration
>   - three-point floor alignment
>   - fitted floor alignment
>   - collinear fit rejection
>   - normal flip
>   - origin maps floor to zero plane
> - In `pipeline.rs`, if navmesh has 0 triangles:
>   - manifest navmesh fields become `null`
>   - stale `navmesh.glb` / `navmesh.bin` are deleted
>   - warning is recorded
>
> ## GUI Finish
> - Remove Floor Mode option `plane`.
> - Keep Floor Mode dropdown only if both `three-point` and `fit` remain visible.
> - Status panel should show:
>   - source loaded
>   - bake stage
>   - warnings
>   - manifest output
>   - artifact actions: Preview WebAR, Save ZIP
> - Keep Editor transform tab because it supports final bake workflow.
>
> ## Docs
> - README must match final state:
>   - GUI supports `.ply/.splat`
>   - CLI/core may support `.sog/meta.json`
>   - recipe examples use `floorPoints` and `floorFitPoints`
>   - no `floorNormal` public recipe
>   - no plane equation mode
> - Add “Completed requirements” mapping:
>   - preprocessing/filtering
>   - mesh reconstruction
>   - occlusion mesh
>   - navmesh
>   - GLB/WebAR export
>   - CLI/GUI packaging
>   - calibration for real-world scale/coordinates
>
> ## Verification
> - `cargo test --workspace`
> - `cd apps/gui && pnpm test`
> - `cd apps/gui && pnpm run build`
> - `cd apps/gui && pnpm run test:e2e`
> - Manual demo:
>   - load `.splat`
>   - pick floor points
>   - pick scale endpoints
>   - bake
>   - verify `manifest.json`, `scene.sog`, `collision_mesh.json`, `occlusion.glb`, `index.html`, `webar.zip`
>   - verify navmesh null behavior when invalid
>
> ## Assumptions
> - Auto floor detection from entire cloud is out of scope for final demo.
> - Fit mode means “fit automatically from user-picked points”.
> - Backend remains responsible for final calibration math and validation.
</details>

Mình sẽ sửa hướng plan: **không remove `floorPoints` / `floorFitPoints` khỏi recipe backend**.

Lý do: nếu backend chỉ “trust `floorNormal` từ GUI”, CLI yếu đi, backend mất khả năng tự validate collinear/coincident points, và logic calibration bị dồn sang frontend. Với đề tài/demo, cách tốt nhất là: **user pick điểm, hệ thống tự fit plane từ các điểm đó**. Không nên auto PCA toàn bộ point cloud trong bản finish này, vì dễ bắt nhầm tường/trần/object clutter thành “floor”.
